name: backend
on: [push, pull_request]
jobs:
  quality:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/pyproject.toml
      - run: pip install -e '.[dev]'
      - run: alembic -c alembic.ini upgrade head
      - run: ruff check .
      - run: ruff format --check .
      - run: mypy app
      - run: bandit -r app
      - run: python -m venv /tmp/causora-audit
      - run: /tmp/causora-audit/bin/python -m pip install --upgrade pip
      - run: /tmp/causora-audit/bin/python -m pip install . pip-audit
      - run: /tmp/causora-audit/bin/pip-audit --skip-editable
      - run: pytest
  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - run: npm run build
      - run: npm audit
